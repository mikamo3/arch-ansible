---
# Container configuration
containers:
  docker:
    enable: true # Install Docker (main runtime)
    compose: true # Install docker-compose for home services

  management_tools:
    enable: true # Install useful GUI/TUI tools

# Docker daemon configuration
docker:
  enable_service: true # Enable Docker service on boot
