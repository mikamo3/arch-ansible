---
# Network role configuration

# Ethernet static IP configuration
# ethernet:
#   interface: "enp7s0"
#   static_ip: "192.168.100.10"
#   netmask: "24"

# Network application control
apply_network_on_reboot: true # Apply static IP configuration on reboot (default)

# Example for different machines:
# For AI server:
#   ethernet:
#     interface: "enp7s0"
#     static_ip: "192.168.100.10"
#     netmask: "24"
# For mainpc:
#   ethernet:
#     interface: "enp3s0"
#     static_ip: "192.168.100.20"
#     netmask: "24"role configuration

# Ethernet static IP configuration
# ethernet_interface: "enp7s0"
# ethernet_static_ip: "192.168.100.10"
# ethernet_netmask: "24"

# Activation control
activate_immediately: false # Set to true to activate during ansible run (risky for remote execution)

# Example for different machines:
# For AI server: ethernet_interface: "enp7s0", ethernet_static_ip: "192.168.100.10"
# For mainpc: ethernet_interface: "enp3s0", ethernet_static_ip: "192.168.100.20"
