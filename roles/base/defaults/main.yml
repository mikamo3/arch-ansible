---
# Default variables for base role - using role-scoped naming

base:
  # Pacman configuration
  parallel_downloads: 5

  # System configuration
  hostname: archlinux # Default hostname (should be overridden in inventory)
