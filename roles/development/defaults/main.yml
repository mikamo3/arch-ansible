---
# Default variables for development role - Software development environment

development:
  # Core development tools installation
  install: false           # Install development tools
  
  # GUI development editor
  install_vscode: true     # Install Visual Studio Code
  
  # Git enhanced tools  
  git_tools: true          # Install Git enhanced tools (GitHub CLI, etc.)
  
  # Container development tools
  containers: false        # Install container development tools (Docker, etc.)
