---
- name: Install chezmoi
  aur:
    name: chezmoi
    state: present
    use: paru
  become: false

- name: Set default shell to zsh
  ansible.builtin.user:
    name: "{{ ansible_user }}"
    shell: /usr/bin/zsh
  become: true
